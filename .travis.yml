sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t kaushaldocid/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kaushaldocid/docker-react npm run test
